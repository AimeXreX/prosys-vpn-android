# ProSyS VPN — V2

این پوشه نسخه مستقل V2 است. پروژه اصلی `ProSySVPN-Android` هنگام ساخت این نسخه تغییر نکرده است.

## هدف تجربه کاربری

- صفحه شروع به داشبورد مینیمال اختصاصی ProSyS تبدیل شده است.
- کاربر برای اتصال فقط یک کنترل مرکزی بزرگ و واضح می‌بیند.
- فهرست سرورها، تب‌ها، جستجو و ابزارهای فنی به صفحه جداگانه «اتصال‌های من» منتقل شده‌اند.
- کارت حجم رایگان فشرده شده و تبلیغ پلن پولی به یک ردیف کم‌مزاحمت تبدیل شده است.
- راهنمای چهارمرحله‌ای در اولین اجرا نمایش داده می‌شود و بعداً از صفحه اصلی در دسترس است.
- «اتصال سریع» تمام اتصال‌های رایگان و شخصی را بررسی می‌کند و سالم‌ترین گزینه با کمترین تأخیر را برمی‌گزیند.
- انتخاب دستی سرور برای کاربران حرفه‌ای همچنان در صفحه اصلی باقی مانده است.
- پیام‌های حیاتی فارسی از عبارت‌های فنی به دستورهای کوتاه و قابل‌فهم تبدیل شده‌اند.
- وضعیت وصل/قطع با متن صریح نمایش داده می‌شود و فقط به رنگ یا آیکن وابسته نیست.
- پیشنهاد پلن پولی در یک کارت مستقل و محترمانه قرار دارد و راه سایت و تلگرام را نشان می‌دهد.

## تغییرات فنی

- `MainViewModel.testAllForQuickConnect()` همه پروفایل‌های موجود را به سرویس تست می‌فرستد.
- `MainViewModel.selectBestMeasuredServer()` کمترین ping مثبت را انتخاب می‌کند و fallback امن دارد.
- اگر سهمیه روزانه تمام شده باشد، پروفایل رایگان مدیریت‌شده از انتخاب خودکار کنار گذاشته می‌شود.
- تست اتصال سریع سقف انتظار ۴۵ ثانیه دارد تا رابط کاربری قفل نشود.
- پیام پایان ناموفق worker نیز حالت testing را می‌بندد؛ نسخه قبلی فقط پایان موفق را مدیریت می‌کرد.
- آیکن و برچسب Quick Settings به هویت ProSyS تغییر کرده است.
- سیاست انتخاب سریع به کلاس خالص `QuickConnectSelector` منتقل شده و برای آن تست واحد نوشته شده است.
- انتخاب خودکار سابقه موفقیت و شکست هر مسیر را در امتیاز نهایی لحاظ می‌کند.
- در شکست شروع اتصال، حداکثر دو مسیر جایگزین به‌صورت کنترل‌شده امتحان می‌شوند.
- کیفیت فنی ping به عبارت‌های «عالی، خوب و ضعیف» تبدیل شده است.
- عیب‌یابی فارسی، حالت خالی هدفمند و بازخورد لمسی اضافه شده‌اند.
- توکن دستگاه با کلید غیرقابل‌استخراج Android Keystore رمز می‌شود و backup برنامه غیرفعال است.
- سایت، ربات فروش و کانال رسمی به‌صورت مسیرهای مستقل در برنامه ثبت شده‌اند.

## بک‌اند

روش استقرار بک‌اند همچنان تک‌پوشه‌ای است. health وابستگی 3x-ui، محدودیت جداگانه ثبت دستگاه، پیام عمومی اختیاری و دستور `npm run backup` اضافه شده‌اند.

## بررسی و انتشار

پیش‌نیاز Gradle نسخه JDK 17 یا جدیدتر است. در این سیستم JDK 21 داخل Android Studio در مسیر زیر قرار دارد:

```text
C:\Program Files\Android\Android Studio\jbr
```

دستورهای اصلی:

```powershell
cd V2rayNG
$env:JAVA_HOME='C:\Program Files\Android\Android Studio\jbr'
.\gradlew.bat :app:testPlaystoreDebugUnitTest
.\gradlew.bat :app:assemblePlaystoreDebug
```

برای انتشار، `versionCode` و `versionName` باید افزایش یابد و همان کلید release نسخه فعلی استفاده شود.
